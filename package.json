{
  "name": "trong",
  "version": "1.2.2",
  "author": "Sergey Popov",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prepublish": "yarn workspace @trong-orm/common-scripts prepublish",
    "postpublish": "yarn workspace @trong-orm/common-scripts postpublish",
    "lint": "yarn turbo run lint",
    "check-typing": "yarn workspaces run check-typing"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "@changesets/cli": "^2.24.2",
    "turbo": "^1.4.3"
  }
}
